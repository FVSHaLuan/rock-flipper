using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum GameAudioChannel
{
    Music = 0,
    UIMusic = 8,
    UI = 4,

    ///
    AllEffect = 3,

    ///
    Effect = 1,
    SpecialEffect = 2,
    TimeWarp = 5,
    QuickDeath = 6,
    Tap = 7,
}
